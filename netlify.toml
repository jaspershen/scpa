[build]
  # Skip Hugo - serve pre-built static files directly
  command = "echo 'Static site - no build required'"
  publish = "public"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
